module matrix.bin.russian.destroy.mathcfloat;

/** 
A related fact is that if {v1 , . . . , vk } is a basis for a vector space of order
n that includes the vector x and x = c1 v1 + · · · ck vk , then x = 0n if and only
if ci = 0 for each i.
**/ 
void main(int argc, const(char)** argv)
{
    values[] argv[wku];
}