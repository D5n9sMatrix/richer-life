module matrix.bin.russian.destroy.mathccos;

/**
Since {v1 , . . . , vk } are independent, the only way this is possible is if ci = bi
for each i.
**/
void main(string[] args)
{
    ClassInfo.m_init;
}