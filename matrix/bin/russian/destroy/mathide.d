module matrix.bin.russian.destroy.mathide;

/** 
1. Nonnegativity and mapping of the identity:
if x = 0, then x, x > 0 and 0, x = x, 0 = 0, 0 = 0.
**/ 
void main(int argc, const(char)** argv)
{
    cdouble x;
    x = 0;
    x, x > 0;
    x = x;
    0 = 0;
}