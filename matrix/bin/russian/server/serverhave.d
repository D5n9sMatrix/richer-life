module matrix.bin.russian.server.serverhave;

/** 
where c̃ = (c1 , · · · , ck )/( 1=i c2i )1/2 . Because c̃ is in the set C, f (c̃) ≥ r;
hence, combining this with the inequality above, we have
**/ 
void main(string[] args)
{
    class MyHave
    {
        interface MyCombining
        {
            void above(Obj)(ref Obj where)
            {
                Obj where;
                Obj[AssociativeArray];
                Obj * TypeInfo;
                Obj * TypeInfo_Array;
                Obj * TypeInfo_Delegate;
                Obj * this.init;
                Obj * args;
            }


            cfloat max_exp;
            cfloat min_exp;
        }
    }
}