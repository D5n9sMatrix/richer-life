module matrix.bin.russian.client.preface.prefacesimple;

/** 
I also sometimes make simple assumptions without stating them explicitly.
For example, I may write “for all i” when i is used as an index to a vector.
I hope it is clear that “for all i” means only “for i that correspond to indices
**/ 
void main(string[] args)
{
    class MyAlso
    {
        interface MySometimes
        {
            void simple(Obj)(ref Obj make)
            {
                Obj make;
                Obj[NameSpaceList];
                AlignAttributes BufferPullFull;
                align (AssignExpression)
                {
                    Obj TypeInfo;                    
                }
                DeprecatedAttributes Error;
                deprecated (AssignExpression)
                {
                    Obj TypeInfoError;
                }
                
            }
        }
    }
}